const baseRed = '#e7292a';

const baseColors = {
  red: baseRed,
  lightGray: '#D1D1D1',
  darkGray: '#353535',
  white: '#ffffff',
  black: '#000000',
};

const colors = {
  ...baseColors,
};

export default colors;
